#pragma once

#include <iostream>
#include <cmath>

namespace difference_of_squares {
    std::size_t  square_of_sum(std::size_t);
    std::size_t  sum_of_squares(std::size_t);
    std::size_t  difference(std::size_t);
}  
